{
    "scripts": {
        "watch:sass": "sass --watch scss:_site/css",
        "build:sass": "sass scss:_site/css",
        "watch:eleventy": "eleventy --serve",
        "build:eleventy": "eleventy",
        "start": "npm-run-all build:sass --parallel watch:*",
        "build": "npm-run-all build:*"
    },
    "devDependencies": {
        "@11ty/eleventy": "^1.0.0",
        "npm-run-all": "^4.1.5",
        "sass": "^1.49.9"
    }
}
